Some *very* useful *small* tools for quicklooks of data.

To-Do:

- Given a sample of (X, Y)
    - Calculate the mean/median/geomean of Y with linear/logarithmic binning in X and make the plot.
    - Calculate various correlations (must have been written)


